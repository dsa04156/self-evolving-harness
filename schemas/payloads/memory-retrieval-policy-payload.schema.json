{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://self-evolving-harness.local/schemas/payloads/memory-retrieval-policy-payload.schema.json",
  "title": "SEH Memory Retrieval Policy Payload v1",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schemaVersion",
    "language",
    "readableNamespaces",
    "queryMode",
    "maxRecords",
    "maxTokens",
    "minimumScore",
    "tieBreak"
  ],
  "properties": {
    "schemaVersion": {
      "const": 1
    },
    "language": {
      "const": "seh.memory-retrieval-policy.v1"
    },
    "readableNamespaces": {
      "type": "array",
      "minItems": 1,
      "maxItems": 16,
      "uniqueItems": true,
      "items": {
        "type": "string",
        "enum": [
          "project_facts",
          "user_preferences",
          "accepted_lessons",
          "rejected_mutations",
          "session_summaries"
        ]
      }
    },
    "queryMode": {
      "type": "string",
      "enum": [
        "recency",
        "lexical",
        "fixed_hybrid"
      ]
    },
    "hybridLexicalWeight": {
      "type": "number",
      "minimum": 0,
      "maximum": 1
    },
    "maxRecords": {
      "type": "integer",
      "minimum": 0,
      "maximum": 128
    },
    "maxTokens": {
      "type": "integer",
      "minimum": 0,
      "maximum": 65536
    },
    "minimumScore": {
      "type": "number",
      "minimum": 0,
      "maximum": 1
    },
    "tieBreak": {
      "type": "string",
      "enum": [
        "record_id_ascending",
        "created_at_then_record_id"
      ]
    }
  },
  "allOf": [
    {
      "if": {
        "properties": {
          "queryMode": {
            "const": "fixed_hybrid"
          }
        },
        "required": [
          "queryMode"
        ]
      },
      "then": {
        "required": [
          "hybridLexicalWeight"
        ]
      },
      "else": {
        "not": {
          "required": [
            "hybridLexicalWeight"
          ]
        }
      }
    }
  ]
}
